CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    product VARCHAR(50),
    quantity INT,
    total_amount DOUBLE,
    status VARCHAR(30)
);

CREATE TABLE inventory (
    product_name VARCHAR(50),
    quantity INT
);
